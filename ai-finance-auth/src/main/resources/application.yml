server:
  port: 8100

spring:
  application.name: ai-finance-auth
  datasource:
    url: jdbc:mysql://localhost:3306/ai_finance?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl